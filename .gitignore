# Build artifacts
build/

# Logs
*.log

# Personal git hook overrides
lefthook-local.y*ml
.lefthook-local/

# Python
.venv/
.venv-wsl/
__pycache__/
.ruff_cache/
.mypy_cache/

# Rust
target/
# WSL builds against a shared Windows-home checkout use CARGO_TARGET_DIR to
# keep their output away from the MSYS2 one; see README's Setup on Windows.
target-wsl/

# Terraform local state and provider cache
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/crash.log

# Agent worktrees
.claude/worktrees/
