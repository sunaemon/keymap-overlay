[package]
name = "keymap-overlay-winui"
version.workspace = true
edition = "2024"
license.workspace = true
repository.workspace = true

# This is an experimental pure-Rust WinUI 3 frontend. The WPF frontend remains
# the release implementation until this has passed the physical focus test.
[target.'cfg(target_os = "windows")'.dependencies]
anyhow = "1.0"
keymap-overlay = { path = "../keymap-overlay" }
log = "0.4.33"
windows = { version = "0.62.2", features = [
  "Win32_Foundation",
  "Win32_Graphics_Dwm",
  "Win32_Graphics_Gdi",
  "Win32_System_LibraryLoader",
  "Win32_UI_HiDpi",
  "Win32_UI_WindowsAndMessaging",
] }
windows-core = { git = "https://github.com/microsoft/windows-rs.git", rev = "845e42a4328ec5b54b97f965798589d997cad177" }
windows-reactor = { git = "https://github.com/microsoft/windows-rs.git", rev = "845e42a4328ec5b54b97f965798589d997cad177" }

[target.'cfg(target_os = "windows")'.build-dependencies]
windows-reactor-setup = { git = "https://github.com/microsoft/windows-rs.git", rev = "845e42a4328ec5b54b97f965798589d997cad177" }

[lints.clippy]
cognitive_complexity = "warn"

[lints.rust]
# WinUI exposes the HWND only through COM, and the non-activating overlay
# contract requires a small, reviewed Win32 subclass around that handle.
unsafe_code = "deny"
